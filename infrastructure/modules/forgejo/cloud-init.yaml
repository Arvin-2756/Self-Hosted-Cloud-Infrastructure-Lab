#cloud-config
package_update: true
packages:
  - ca-certificates
  - curl
  - git
  - openssh-server
runcmd:
  - systemctl enable --now ssh
  - echo 'Forgejo container ready for application deployment' > /etc/motd
